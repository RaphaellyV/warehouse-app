<section id='suppliers'>
    <h2>Fornecedores</h2>

    <%@suppliers.each do |s|%>
      <%= s.brand_name%>
      <%= s.city%> - <%= s.state%>
    <%end%>

    <%if @suppliers.empty?%>
        <p>Não existem fornecedores cadastrados.</p>
    <%end%>
</section>